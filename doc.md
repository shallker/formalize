checkbox
  .disabled
  .checked
  .defaultChecked
  .form
    .id
  .name
  .type
  .value

button
  .disabled
  .form
    .id
  .name
  .type
  .value

radio
  .checked
  .defaultChecked
  .disabled
  .form
    .id
  .name
  .type
  .value

select
  .options
    [index]
    .length
    .selectedIndex
    .add(ele, [index])
    .item(index)
    .namedItem(name)
    .remove(index)
  .disabled
  .form
    .id
  .length
  .multiple
  .name
  .selectedIndex
  .size
  .type
  .add()
  .remove()
